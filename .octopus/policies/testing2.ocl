name = "testing2"
violation_action = "block"

conditions {
    rego = <<-EOT
            package testing2
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package testing2
            
            default evaluate := true
            EOT
}