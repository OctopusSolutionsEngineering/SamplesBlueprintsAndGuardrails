name = "hello-policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package hello_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package hello_policy
            
            default evaluate := true
            EOT
}